# OpenRouter — free LLM API (openrouter.ai)
OPENROUTER_API_KEY=your_key_here

# HuggingFace — for pulling fine-tuned model
HF_TOKEN=your_token_here

# Model repo on HuggingFace Hub (Member 1 sets this after training)
HF_MODEL_REPO=your-username/reportraahat-flan-t5

# CORS — set to Vercel URL in production
FRONTEND_URL=http://localhost:3000
