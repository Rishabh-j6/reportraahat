// OWNER: Member 2
// Landing page — Aurora background, drag-and-drop upload zone,
// language toggle (EN / HI), "Samjho!" button
// Calls POST /api/analyze-report on submit

export default function HomePage() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <p className="text-primary text-2xl font-bold">
        Upload Page — Member 2
      </p>
    </main>
  )
}
